# ❤️ 손하트 인식 튜닝 가이드

## 🎯 목적
`gesture-tuner.html`을 사용하여 손하트 인식의 임계값을 실시간으로 조정하고 최적의 값을 찾습니다.

---

## 🚀 빠른 시작

### 1. 튜너 실행
```bash
cd 05_S14PD105_ai_motion_model_test/echoforest-ai/motion-model-test
# VS Code에서 gesture-tuner.html을 Live Server로 실행
```

### 2. 초기화 순서
1. **1️⃣ 초기화** 버튼 클릭 → MediaPipe 모델 로드
2. **2️⃣ 카메라** 버튼 클릭 → 카메라 권한 허용
3. **3️⃣ 시작** 버튼 클릭 → 실시간 감지 시작

---

## 🤝 손하트 튜닝 방법

### 핵심 패널: **"🤝 양손 간 거리 측정"**

이 패널에서 양손의 특정 랜드마크 간 거리를 실시간으로 측정할 수 있습니다.

#### 손하트 인식 조건
손하트는 **두 가지 접점**이 필요합니다:
1. **엄지↔엄지** (양손 엄지 끝이 만남)
2. **검지↔검지** (양손 검지 끝이 만남)

#### 프리셋 버튼 활용
- **엄지↔엄지 (탈모빔)**: 양손 엄지 끝(4번) 간 거리
- **검지↔검지**: 양손 검지 끝(8번) 간 거리
- **엄지↔검지 (하트)**: 손1 엄지 ↔ 손2 검지 (교차 측정용)

---

## 📊 튜닝 절차

### Step 1: 엄지↔엄지 거리 측정

1. **프리셋 버튼 클릭**: "엄지↔엄지 (탈모빔)"
2. **손하트 포즈 취하기**: 양손으로 하트 모양 만들기
3. **거리 확인**:
   - **거리 (raw)**: 실제 3D 거리
   - **정규화**: 손바닥 크기로 나눈 값 (이 값을 사용!)

4. **임계값 조정**:
   - 슬라이더로 임계값 조정 (기본: 0.15)
   - 하트 포즈에서 "✅ 가까움!" 표시되도록 조정
   - 하트가 아닐 때 "❌ 멀음" 표시되도록 확인

5. **최적값 기록**:
   ```
   엄지↔엄지 정규화 거리: ______
   권장 임계값: ______
   ```

### Step 2: 검지↔검지 거리 측정

1. **프리셋 버튼 클릭**: "검지↔검지"
2. **동일한 절차 반복**
3. **최적값 기록**:
   ```
   검지↔검지 정규화 거리: ______
   권장 임계값: ______
   ```

### Step 3: 다양한 시나리오 테스트

다음 상황에서 모두 테스트하세요:

#### ✅ 인식되어야 하는 경우
- [ ] 정확한 하트 포즈
- [ ] 손가락이 살짝 떨어진 하트
- [ ] 손 크기가 큰 사람
- [ ] 손 크기가 작은 사람
- [ ] 카메라와 가까운 거리
- [ ] 카메라와 먼 거리

#### ❌ 인식되지 말아야 하는 경우
- [ ] 손가락을 펴고 있을 때
- [ ] 한 손만 있을 때
- [ ] 하트가 아닌 다른 제스처
- [ ] 중지/약지를 펴고 있는 하트

---

## 🎛️ 실시간 디버깅 도구

### 1. **📊 실시간 수치** 패널
- **엄지-검지 거리**: 한 손의 엄지-검지 거리 (OK 제스처 판별용)
- **손바닥 크기**: 정규화 기준값
- **정규화 거리**: 손바닥 크기로 나눈 거리

### 2. **🖐️ 손가락 상태** 패널
- 각 손가락이 펴져 있는지 접혀 있는지 실시간 표시
- 하트 포즈에서 어떤 손가락이 펴져 있는지 확인

### 3. **📊 손가락 끝 거리 매트릭스**
- 모든 손가락 끝 간의 거리를 5x5 매트릭스로 표시
- 하트 포즈의 전체적인 손 모양 파악 가능

### 4. **📸 스냅샷 캡처**
- 특정 포즈의 랜드마크 데이터를 CSV로 저장
- 나중에 분석하거나 머신러닝 학습 데이터로 활용

---

## 🔧 HeartGesture.js 적용

튜닝이 완료되면 `HeartGesture.js`에 값을 적용합니다.

### 현재 코드 (05_S14PD105_motion_tuning_heart)
```javascript
// HeartGesture.js (6-9번째 줄)
this.thresholds = {
    tipDistance: 0.15, // 👈 이 값을 조정
    ...config
};
```

### 적용 방법
```javascript
// 튜너에서 측정한 값 적용
this.thresholds = {
    tipDistance: 0.18, // 예시: 튜너에서 0.18이 최적이라고 판단됨
    ...config
};
```

### 두 거리가 다를 경우
만약 엄지↔엄지와 검지↔검지의 최적 임계값이 다르다면:

```javascript
// HeartGesture.js 수정 (더 세밀한 제어)
this.thresholds = {
    thumbDistance: 0.15,  // 엄지↔엄지 임계값
    indexDistance: 0.18,  // 검지↔검지 임계값
    ...config
};

// check() 메서드 수정 (33-34번째 줄)
const isTouching = 
    thumbDist < this.thresholds.thumbDistance &&
    indexDist < this.thresholds.indexDistance;
```

---

## 📈 권장 튜닝 전략

### 1. **보수적 접근** (엄격한 인식)
```javascript
tipDistance: 0.12  // 손가락이 거의 붙어야 인식
```
- **장점**: 오인식 감소
- **단점**: 사용자가 정확히 해야 함

### 2. **관대한 접근** (쉬운 인식)
```javascript
tipDistance: 0.20  // 손가락이 좀 떨어져도 인식
```
- **장점**: 사용자 편의성 증가
- **단점**: 다른 제스처와 혼동 가능

### 3. **균형 접근** (권장)
```javascript
tipDistance: 0.15  // 기본값, 대부분의 경우 적절
```

---

## 🎯 최종 체크리스트

튜닝 완료 전 확인사항:

- [ ] 다양한 손 크기에서 테스트 (최소 3명)
- [ ] 다양한 거리에서 테스트 (30cm ~ 100cm)
- [ ] 중지/약지를 펴고 있어도 인식되는지 확인
- [ ] 다른 제스처(OK, V사인 등)와 구분되는지 확인
- [ ] 실시간 FPS가 30 이상 유지되는지 확인
- [ ] 튜닝된 값을 `HeartGesture.js`에 적용
- [ ] 메인 프로젝트에서 최종 테스트

---

## 💡 팁

### 거리 매트릭스 활용
- 하트 포즈를 취했을 때 **손가락 끝 거리 매트릭스**를 확인
- 엄지(4)와 검지(8)의 값이 작게 나오는지 확인
- 다른 손가락(12, 16, 20)의 값과 비교

### 스냅샷 캡처 활용
1. 다양한 하트 포즈를 캡처
2. CSV 다운로드
3. 엑셀에서 열어 통계 분석
4. 평균값과 표준편차를 기반으로 임계값 결정

### 실시간 로그 확인
- 오른쪽 하단 "📝 감지 로그" 패널에서 인식 실패 이유 확인
- "Fingers too far", "Hands upside down" 등의 메시지 참고

---

## 🔗 관련 파일

- **튜너 도구**: `05_S14PD105_ai_motion_model_test/echoforest-ai/motion-model-test/gesture-tuner.html`
- **적용 대상**: `05_S14PD105_motion_tuning_heart/echoforest-ai/motion-model-test/js/gestures/HeartGesture.js`
- **테스트 페이지**: `05_S14PD105_motion_tuning_heart/echoforest-ai/motion-model-test/index.html`

---

## 📞 문제 해결

### 카메라가 안 켜져요
- HTTPS 또는 localhost에서만 카메라 API 작동
- Live Server 사용 필수

### 손이 인식 안 돼요
- 조명이 충분한지 확인
- 손이 카메라 화면에 완전히 들어오는지 확인
- FPS가 낮으면 브라우저 재시작

### 값이 너무 자주 바뀌어요
- 손을 안정적으로 유지
- 여러 번 측정 후 평균값 사용
- 스냅샷 캡처 후 CSV 분석 권장
