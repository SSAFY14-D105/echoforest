# 🎯 gesture-tuner.html 사용성 개선 완료

## ✅ 추가된 기능

### 1. 스페이스바 캡처 기능 ⌨️

**문제**: 양손으로 자세를 잡고 있으면 마우스로 캡처 버튼을 클릭하기 어려움

**해결**: 스페이스바를 눌러 캡처 가능하도록 개선

```javascript
// 스페이스바로 캡처 (팔꿈치로 누르기 쉽게)
document.addEventListener('keydown', (e) => {
    if (e.code === 'Space' && e.target.tagName !== 'INPUT') {
        e.preventDefault();
        captureSnapshot();
        // 시각적 피드백 (버튼 애니메이션)
        const btn = document.getElementById('btnCapture');
        btn.style.transform = 'scale(0.95)';
        setTimeout(() => btn.style.transform = 'scale(1)', 100);
    }
});
```

**사용법**:
1. 양손으로 제스처 포즈 취하기
2. **스페이스바를 팔꿈치로 누르기** 👈
3. 자동으로 캡처되고 버튼이 깜빡임 (시각적 피드백)

**주의**: 라벨 입력창에 포커스가 있을 때는 스페이스바가 일반 입력으로 동작합니다.

---

### 2. 양손 간 거리 시각화 📏

**문제**: 양손으로 제스처할 때 같은 손가락끼리 거리가 얼마나 되는지 알기 어려움

**해결**: 스켈레톤 프리뷰에 양손 간 같은 손가락 거리를 자동으로 표시

#### 시각화 내용
- **엄지↔엄지** (4번↔4번)
- **검지↔검지** (8번↔8번)
- **중지↔중지** (12번↔12번)
- **약지↔약지** (16번↔16번)
- **새끼↔새끼** (20번↔20번)

#### 색상 코드
| 정규화 거리 | 색상 | 의미 | 선 굵기 |
|------------|------|------|---------|
| < 0.15 | 🟢 초록 | 매우 가까움 (하트 인식 가능) | 굵음 (3px) |
| 0.15 ~ 0.3 | 🟡 노랑 | 가까움 | 중간 (2px) |
| > 0.3 | ⚪ 회색 | 멀음 | 얇음 (1px) |

#### 거리 표시
각 연결선 중간에 **정규화된 거리 값**이 숫자로 표시됩니다.

예시:
```
엄지↔엄지: 0.12 (초록색 굵은 선)
검지↔검지: 0.18 (노란색 중간 선)
중지↔중지: 0.45 (회색 얇은 선)
```

---

## 🎨 UI 개선

### 캡처 버튼
**Before**: `📸 캡처`  
**After**: `📸 캡처 (Space)`

### 추가된 팁 메시지
```
💡 팁: 스페이스바를 눌러 캡처하세요 (팔꿈치로 누르기 쉬움)
```

---

## 🧪 사용 시나리오

### 손하트 튜닝 예시

1. **gesture-tuner.html 실행**
   ```bash
   cd 05_S14PD105_ai_motion_model_test/echoforest-ai/motion-model-test
   # VS Code에서 gesture-tuner.html을 Live Server로 실행
   ```

2. **초기화**
   - 1️⃣ 초기화 → 2️⃣ 카메라 → 3️⃣ 시작

3. **양손 하트 포즈 취하기**
   - 양손으로 하트 모양 만들기
   - 스켈레톤 프리뷰에서 실시간으로 거리 확인
   - **엄지↔엄지**: 초록색 (0.12) ✅
   - **검지↔검지**: 초록색 (0.14) ✅

4. **스페이스바로 캡처**
   - 팔꿈치로 스페이스바 누르기
   - 버튼이 깜빡이며 캡처 완료!

5. **다양한 포즈 캡처**
   - 라벨 입력: "하트_정확", "하트_느슨", "하트_손가락펴짐" 등
   - 각 포즈마다 스페이스바로 캡처

6. **CSV 다운로드**
   - 📥 CSV 다운로드 버튼 클릭
   - Excel에서 열어 분석 (한글 정상 표시 ✅)

---

## 📊 실시간 디버깅 활용

### 손하트 조건 확인

스켈레톤 프리뷰를 보면서:

1. **엄지↔엄지 거리** 확인
   - 초록색(< 0.15)이어야 함
   - 숫자 확인: 0.12 정도가 이상적

2. **검지↔검지 거리** 확인
   - 초록색(< 0.15)이어야 함
   - 숫자 확인: 0.14 정도가 이상적

3. **다른 손가락들** 확인
   - 중지↔중지: 노랑~회색 (0.2~0.4)
   - 약지↔약지: 노랑~회색 (0.2~0.4)
   - 새끼↔새끼: 노랑~회색 (0.2~0.4)

4. **손가락 클러스터** 확인
   - 한 손의 검지/중지/약지/새끼가 모여있는지
   - 📊 손가락 끝 거리 매트릭스 패널에서 확인

---

## 🎯 튜닝 팁

### 임계값 찾기

1. **정확한 하트 포즈 10번 캡처**
   - 라벨: "하트_정확"
   - 스페이스바로 빠르게 캡처

2. **느슨한 하트 포즈 10번 캡처**
   - 라벨: "하트_느슨"
   - 손가락을 살짝 떨어뜨림

3. **손가락 펴진 포즈 10번 캡처**
   - 라벨: "하트_손가락펴짐"
   - 중지/약지를 펴고 하트 모양

4. **CSV 다운로드 및 분석**
   - Excel에서 열기
   - 각 라벨별 평균 거리 계산
   - 임계값 결정

### 예시 분석 결과

| 라벨 | 엄지↔엄지 평균 | 검지↔검지 평균 | 권장 임계값 |
|------|---------------|---------------|------------|
| 하트_정확 | 0.12 | 0.14 | 0.15 |
| 하트_느슨 | 0.18 | 0.20 | 0.20 (관대) |
| 하트_손가락펴짐 | 0.15 | 0.16 | - (거부) |

---

## 🔧 기술 세부사항

### 거리 계산 방식

```javascript
// 3D 유클리드 거리
const dist3D = Math.sqrt(
    Math.pow(hand1[tipIdx].x - hand2[tipIdx].x, 2) +
    Math.pow(hand1[tipIdx].y - hand2[tipIdx].y, 2) +
    Math.pow(hand1[tipIdx].z - hand2[tipIdx].z, 2)
);

// 정규화 (양손 평균 손바닥 크기로 나눔)
const avgPalm = (palm1 + palm2) / 2;
const normDist = dist3D / avgPalm;
```

### 시각화 로직

```javascript
// 거리에 따라 색상 및 굵기 변경
if (normDist < 0.15) {
    pctx.strokeStyle = '#4ade80'; // 초록
    pctx.lineWidth = 3;
} else if (normDist < 0.3) {
    pctx.strokeStyle = '#fbbf24'; // 노랑
    pctx.lineWidth = 2;
} else {
    pctx.strokeStyle = '#888'; // 회색
    pctx.lineWidth = 1;
}
pctx.setLineDash([5, 5]); // 점선
```

---

## 📚 관련 파일

- **튜너**: `05_S14PD105_ai_motion_model_test/echoforest-ai/motion-model-test/gesture-tuner.html`
- **로직**: `05_S14PD105_motion_tuning_heart/echoforest-ai/motion-model-test/js/gestures/HeartGesture.js`
- **가이드**: `05_S14PD105_ai_motion_model_test/HEART_TUNING_GUIDE.md`

---

## ✅ 체크리스트

- [x] 스페이스바 캡처 기능 추가
- [x] 양손 간 거리 시각화 추가
- [x] 거리에 따른 색상 코드 적용
- [x] 정규화된 거리 값 표시
- [x] 시각적 피드백 (버튼 애니메이션)
- [x] UI 개선 (팁 메시지 추가)
- [ ] 실제 손하트 포즈로 테스트 (권장)
- [ ] 다양한 포즈 캡처 및 CSV 분석 (권장)

---

## 🚀 다음 단계

1. gesture-tuner.html로 다양한 손하트 포즈 캡처
2. CSV 다운로드 후 Excel에서 분석
3. 최적 임계값 결정
4. HeartGesture.js에 적용
5. index.html로 최종 테스트
