# raw_audio — YouTube 오디오 저장 위치 (커밋 제외)

`scripts/01_youtube_downloader.py`가 받은 `.wav`가 여기 쌓입니다. **용량이 커서 git에는 커밋하지 않습니다**(`.gitkeep`만 추적).

재현하려면 `scripts/00_url_list.txt`의 URL로 `01_youtube_downloader.py`를 돌리면 이 폴더가 채워집니다.
