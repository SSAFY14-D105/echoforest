# 06_ai_labeled — AI 자동 라벨 (8라벨 시도)

`scripts/11_ai_labeling.py`가 Gemini로 STT 문장에 8라벨을 자동 부여한 결과.

- `gemini_labeled_17k.tsv` — `sentence` + 8라벨 + `source`

> 8라벨 체계 미채택. 최종 라벨링은 키워드 기반 10라벨([`../../labeling/`](../../labeling))로 대체됐습니다. 기록용입니다.
