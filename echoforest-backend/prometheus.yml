global:
  scrape_interval: 15s # 15초마다 수집

scrape_configs:
  - job_name: 'echoforest-backend'
    metrics_path: '/actuator/prometheus'
    scheme: 'http'
    static_configs:
      - targets: ['game-server:9001'] # Docker Network 내부 DNS 사용
