# ==========================================
# 1. 빌드 캐시 및 결과물 제외
# ==========================================

build
.gradle
out

# ==========================================
# 2. IDE 및 Git 설정 파일 제외
# ==========================================

.idea
.vscode
*.iml
.git
.gitignore

# ==========================================
# 3. [핵심] 런타임 데이터 폴더 제외 (권한 에러 해결)
# ==========================================

# 젠킨스 데이터 (root 권한 파일들이라 빌드 시 에러 발생함)

jenkins_home

# DB 및 Redis 데이터

mysql_data
redis_data

# 사용자가 업로드한 이미지 등 (이미지에 포함되면 용량 커짐)

uploads

# ==========================================
# 4. 도커 이미지에 불필요한 설정 파일들
# ==========================================

docker-compose.yml
Dockerfile
nginx.conf
README.md