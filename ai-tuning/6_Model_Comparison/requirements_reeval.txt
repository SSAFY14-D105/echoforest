# 클린 테스트셋(181) 재평가용 의존성
# 학습 환경(Setting/Jupyter_spec.md)과 동일 버전 — 저장된 모델 로딩 호환을 위해 버전 고정 권장
#
# ⚠️ torch는 CUDA 빌드라 아래처럼 별도 설치 (드라이버가 CUDA 12.1+ 지원 시):
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   (드라이버가 낮으면 cu118 등으로: --index-url https://download.pytorch.org/whl/cu118)

transformers==5.0.0
datasets==4.5.0
accelerate==1.12.0
scikit-learn==1.8.0
pandas
matplotlib
seaborn
peft
