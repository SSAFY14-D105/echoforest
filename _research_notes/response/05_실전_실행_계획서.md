# 🚀 Unsmile 파인튜닝 실전 실행 계획서 (2주 완성)

> **목표**: 2주 안에 게임 내 팀 사기 저하 탐지 모델을 실제 배포 가능한 수준까지 완성

---

## 📅 전체 일정 (10 작업일)

| 주차 | 일정 | 핵심 목표 | 산출물 |
|------|------|-----------|--------|
| **Week 1** | Day 1-5 | 데이터 수집 및 전처리 파이프라인 완성 | 최소 500개 라벨링 데이터 |
| **Week 2** | Day 6-10 | 모델 학습 및 평가, 프론트엔드 최적화 | 배포 가능한 모델 v1.0 |

---

## 🎯 성공 기준 (Success Criteria)

### 최소 목표 (Must Have)
- [ ] negative_morale 라벨 F1-score **≥ 0.70**
- [ ] 기존 Unsmile 라벨 평균 F1-score **≥ 0.80** (원본 대비 90% 유지)
- [ ] 추론 속도 **< 200ms** per sentence (실시간성 확보)

### 목표 (Should Have)
- [ ] negative_morale F1-score **≥ 0.75**
- [ ] 기존 라벨 평균 F1-score **≥ 0.85**
- [ ] 브라우저 렉 개선 (FPS 45 → 55 이상)

### 이상적 (Nice to Have)
- [ ] negative_morale F1-score **≥ 0.80**
- [ ] 전체 Macro F1-score **≥ 0.83**
- [ ] 게임 내 실제 플레이 테스트 완료

---

## 📁 프로젝트 구조

```
fine-turning/
├── data_collection/
│   ├── audio_transcriber_web.py          # [수정 필요] VAD 추가
│   ├── youtube_downloader.py
│   └── raw_data/                         # 수집된 텍스트 저장
│
├── preprocessing/
│   ├── vad_chunking.py                   # [신규] VAD 기반 청킹
│   ├── data_augmentation.py              # [신규] 노이즈 주입
│   ├── text_normalization.py             # [신규] 자음 복원
│   └── dataset_builder.py                # [신규] 최종 데이터셋 생성
│
├── training/
│   ├── train.py                          # [신규] 메인 학습 스크립트
│   ├── config.yaml                       # [신규] 학습 설정
│   └── checkpoints/                      # 모델 체크포인트 저장
│
├── evaluation/
│   ├── evaluate.py                       # [신규] 평가 스크립트
│   └── confusion_matrix.png              # 혼동 행렬 시각화
│
├── models/
│   └── unsmile_v1.0/                     # 최종 모델
│
└── requirements.txt                       # [신규] 의존성 패키지
```

---

## 📋 Week 1: 데이터 파이프라인 구축 (Day 1-5)

### Day 1: 환경 설정 및 VAD 구현

#### ✅ 체크리스트
- [ ] Python 가상환경 생성
- [ ] 필수 패키지 설치
- [ ] VAD 청킹 스크립트 작성 및 테스트

#### 🔧 구현 코드

**requirements.txt 생성**:
```txt
# STT 및 오디오 처리
SpeechRecognition==3.10.0
pydub==0.25.1
webrtcvad==2.0.10
pyaudio==0.2.13

# 데이터 처리
pandas==2.0.3
numpy==1.24.3
scikit-learn==1.3.0

# 딥러닝
torch==2.1.0
transformers==4.35.0
datasets==2.14.5

# 유틸리티
tqdm==4.66.1
pyyaml==6.0.1
matplotlib==3.7.2
seaborn==0.12.2
```

**설치 명령어**:
```bash
# 가상환경 생성
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 패키지 설치
pip install -r requirements.txt
```

**preprocessing/vad_chunking.py 작성**:
```python
import webrtcvad
from pydub import AudioSegment
from pydub.silence import split_on_silence
import os

class VADChunker:
    def __init__(self, aggressiveness=3, min_duration=0.5, max_duration=15.0):
        """
        Args:
            aggressiveness: VAD 민감도 (0-3, 3이 가장 공격적)
            min_duration: 최소 청크 길이 (초)
            max_duration: 최대 청크 길이 (초)
        """
        self.vad = webrtcvad.Vad(aggressiveness)
        self.min_duration = min_duration * 1000  # ms로 변환
        self.max_duration = max_duration * 1000

    def chunk_audio(self, audio_file_path):
        """오디오 파일을 VAD로 청킹"""
        print(f"Processing: {audio_file_path}")

        # 오디오 로드
        audio = AudioSegment.from_file(audio_file_path)

        # 샘플링 레이트 16000Hz로 변환 (VAD 요구사항)
        audio = audio.set_frame_rate(16000).set_channels(1)

        # 간단한 silence 기반 1차 분할 (VAD 전처리)
        chunks = split_on_silence(
            audio,
            min_silence_len=300,  # 300ms 침묵
            silence_thresh=audio.dBFS - 14,
            keep_silence=200
        )

        # 길이 필터링
        filtered_chunks = []
        for chunk in chunks:
            duration = len(chunk)
            if self.min_duration <= duration <= self.max_duration:
                filtered_chunks.append(chunk)

        print(f"Total chunks: {len(chunks)}")
        print(f"Filtered chunks: {len(filtered_chunks)}")
        print(f"Removal rate: {(1 - len(filtered_chunks)/len(chunks))*100:.1f}%")

        return filtered_chunks

    def save_chunks(self, chunks, output_dir):
        """청크를 파일로 저장"""
        os.makedirs(output_dir, exist_ok=True)

        for i, chunk in enumerate(chunks):
            output_path = os.path.join(output_dir, f"chunk_{i:04d}.wav")
            chunk.export(output_path, format="wav")

        print(f"Saved {len(chunks)} chunks to {output_dir}")

# 사용 예시
if __name__ == "__main__":
    chunker = VADChunker(aggressiveness=3, min_duration=0.5, max_duration=15.0)

    # 테스트
    audio_file = "data_collection/raw_data/test_audio.mp3"
    chunks = chunker.chunk_audio(audio_file)
    chunker.save_chunks(chunks, "data_collection/chunks/")
```

**실행 및 테스트**:
```bash
cd fine-turning
python preprocessing/vad_chunking.py
```

---

### Day 2: STT 변환 및 데이터 수집

#### ✅ 체크리스트
- [ ] 기존 `audio_transcriber_web.py` VAD 적용
- [ ] 유튜브 영상 5-10개 다운로드
- [ ] STT 변환 실행 (목표: 500-700개 발화)

#### 🔧 구현 코드

**data_collection/audio_transcriber_web.py 수정**:
```python
import speech_recognition as sr
from pydub import AudioSegment
import sys
sys.path.append('..')
from preprocessing.vad_chunking import VADChunker
import os

class AudioTranscriber:
    def __init__(self):
        self.recognizer = sr.Recognizer()
        self.chunker = VADChunker(aggressiveness=3, min_duration=0.5, max_duration=15.0)

    def transcribe_youtube_audio(self, audio_file, output_txt):
        """유튜브 오디오를 텍스트로 변환"""

        # 1. VAD 청킹
        chunks = self.chunker.chunk_audio(audio_file)

        # 2. STT 변환
        transcriptions = []
        print(f"\nTranscribing {len(chunks)} chunks...")

        for i, chunk in enumerate(chunks):
            try:
                # 임시 파일로 저장
                temp_path = f"temp_chunk_{i}.wav"
                chunk.export(temp_path, format="wav")

                # STT
                with sr.AudioFile(temp_path) as source:
                    audio_data = self.recognizer.record(source)
                    text = self.recognizer.recognize_google(audio_data, language='ko-KR')
                    transcriptions.append(text)
                    print(f"[{i+1}/{len(chunks)}] {text}")

                # 임시 파일 삭제
                os.remove(temp_path)

            except sr.UnknownValueError:
                print(f"[{i+1}/{len(chunks)}] [인식 실패]")
            except sr.RequestError as e:
                print(f"[{i+1}/{len(chunks)}] [API 오류: {e}]")

        # 3. 저장
        with open(output_txt, 'w', encoding='utf-8') as f:
            for text in transcriptions:
                f.write(text + '\n')

        print(f"\n✅ Saved {len(transcriptions)} transcriptions to {output_txt}")
        return transcriptions

# 실행
if __name__ == "__main__":
    transcriber = AudioTranscriber()

    # 유튜브 영상들 처리
    audio_files = [
        "raw_data/youtube_1.mp3",
        "raw_data/youtube_2.mp3",
        # ... 추가
    ]

    for audio_file in audio_files:
        output_txt = audio_file.replace('.mp3', '_transcript.txt')
        transcriber.transcribe_youtube_audio(audio_file, output_txt)
```

**실행**:
```bash
python data_collection/audio_transcriber_web.py
```

---

### Day 3: 데이터 증강 구현

#### ✅ 체크리스트
- [ ] 노이즈 주입 함수 작성
- [ ] 자음 복원 함수 작성
- [ ] 전체 데이터에 적용 및 검증

#### 🔧 구현 코드

**preprocessing/data_augmentation.py**:
```python
import random

class DataAugmenter:
    def __init__(self, aug_ratio=0.3):
        """
        Args:
            aug_ratio: 증강 데이터 생성 비율 (0.3 = 30%)
        """
        self.aug_ratio = aug_ratio

    def simulate_stt_errors(self, text):
        """Web Speech API 스트리밍 에러 시뮬레이션"""
        words = text.split()
        if len(words) < 3:
            return text

        aug_type = random.random()

        # 60% - 문장 끝 생략
        if aug_type < 0.6:
            drop_count = random.randint(1, min(2, len(words)-1))
            return ' '.join(words[:-drop_count])

        # 30% - 중간 단어 생략
        elif aug_type < 0.9:
            idx = random.randint(1, len(words)-2)
            return ' '.join(words[:idx] + words[idx+1:])

        # 10% - 문장 앞 생략
        else:
            return ' '.join(words[1:])

    def augment_dataset(self, texts, labels):
        """데이터셋 증강"""
        augmented_texts = []
        augmented_labels = []

        for text, label in zip(texts, labels):
            # 원본 추가
            augmented_texts.append(text)
            augmented_labels.append(label)

            # 30% 확률로 증강 버전 추가
            if random.random() < self.aug_ratio:
                aug_text = self.simulate_stt_errors(text)
                augmented_texts.append(aug_text)
                augmented_labels.append(label)

        print(f"Original: {len(texts)} → Augmented: {len(augmented_texts)}")
        return augmented_texts, augmented_labels

# 테스트
if __name__ == "__main__":
    augmenter = DataAugmenter(aug_ratio=0.3)

    test_text = "진짜 왜 저렇게 플레이 하는 거야 답답하네"
    print(f"Original: {test_text}")

    for i in range(5):
        aug = augmenter.simulate_stt_errors(test_text)
        print(f"Aug {i+1}: {aug}")
```

**preprocessing/text_normalization.py**:
```python
import re

class TextNormalizer:
    def __init__(self):
        # 자음 복원 규칙
        self.consonant_rules = {
            r'ㅋ{2,}': '하하',      # ㅋㅋ, ㅋㅋㅋ → 하하
            r'ㅎ{2,}': '하하',      # ㅎㅎ → 하하
            r'ㅉㅉ': '쯧쯧',        # ㅉㅉ → 쯧쯧
            r'ㅇㅇ': '응응',        # ㅇㅇ → 응응
            r'ㅅㅂ': '',           # ㅅㅂ → 삭제 (욕설은 라벨로 처리)
        }

    def normalize(self, text):
        """텍스트 정규화"""
        # 1. 자음 복원
        for pattern, replacement in self.consonant_rules.items():
            text = re.sub(pattern, replacement, text)

        # 2. 연속 공백 제거
        text = re.sub(r'\s+', ' ', text).strip()

        return text

    def batch_normalize(self, texts):
        """배치 정규화"""
        return [self.normalize(text) for text in texts]

# 테스트
if __name__ == "__main__":
    normalizer = TextNormalizer()

    test_texts = [
        "아 ㅋㅋㅋ 진짜 웃기네",
        "ㅉㅉ 너무 답답하다",
        "ㅇㅇ 알겠어"
    ]

    for text in test_texts:
        normalized = normalizer.normalize(text)
        print(f"{text} → {normalized}")
```

**실행**:
```bash
python preprocessing/data_augmentation.py
python preprocessing/text_normalization.py
```

---

### Day 4-5: 라벨링 및 데이터셋 구축

#### ✅ 체크리스트
- [ ] negative_morale 라벨링 가이드라인 작성
- [ ] 최소 700개 데이터 라벨링 (팀원 분업)
- [ ] CSV 형식으로 저장
- [ ] Train/Val/Test 분할 (70/15/15)

#### 📝 라벨링 가이드라인

**negative_morale 판단 기준**:
```
✅ negative_morale로 라벨링:
- 팀원 비난: "너 왜 그렇게 못해", "나만 하네", "혼자 다 해야 되나"
- 포기 발언: "이거 망했다", "안되겠다", "이미 늦었어"
- 짜증/불만: "진짜 답답하네", "아 진짜", "하"
- 비꼬기: "와 대단하시네", "진짜 잘하신다"

❌ negative_morale 아님:
- 단순 감탄사: "아", "어", "음"
- 게임 상황 설명: "저기 적 있어", "왼쪽 가자"
- 긍정적 반응: "좋아", "잘했어", "ㅋㅋㅋ"
```

#### 🔧 데이터셋 구축 코드

**preprocessing/dataset_builder.py**:
```python
import pandas as pd
from sklearn.model_selection import train_test_split
from text_normalization import TextNormalizer
from data_augmentation import DataAugmenter

class DatasetBuilder:
    def __init__(self):
        self.normalizer = TextNormalizer()
        self.augmenter = DataAugmenter(aug_ratio=0.3)

    def load_unsmile_data(self, path="unsmile_dataset.csv"):
        """Unsmile 원본 데이터 로드"""
        df = pd.read_csv(path)
        return df

    def load_custom_data(self, path="custom_labeled_data.csv"):
        """직접 라벨링한 데이터 로드"""
        df = pd.read_csv(path)
        # 텍스트 정규화
        df['text'] = self.normalizer.batch_normalize(df['text'].tolist())
        return df

    def oversample_custom_data(self, df, factor=5):
        """소수 클래스 오버샘플링"""
        return pd.concat([df] * factor, ignore_index=True)

    def build_final_dataset(self, unsmile_path, custom_path, output_dir="dataset"):
        """최종 데이터셋 구축"""

        # 1. 데이터 로드
        unsmile_df = self.load_unsmile_data(unsmile_path)
        custom_df = self.load_custom_data(custom_path)

        # 2. 오버샘플링
        custom_df = self.oversample_custom_data(custom_df, factor=5)
        print(f"Unsmile: {len(unsmile_df)}, Custom (oversampled): {len(custom_df)}")

        # 3. 병합
        final_df = pd.concat([unsmile_df, custom_df], ignore_index=True)

        # 4. 데이터 증강
        texts = final_df['text'].tolist()
        labels = final_df['label'].tolist()
        aug_texts, aug_labels = self.augmenter.augment_dataset(texts, labels)

        final_df = pd.DataFrame({'text': aug_texts, 'label': aug_labels})

        # 5. Train/Val/Test 분할
        train, temp = train_test_split(final_df, test_size=0.3, stratify=final_df['label'], random_state=42)
        val, test = train_test_split(temp, test_size=0.5, stratify=temp['label'], random_state=42)

        # 6. 저장
        os.makedirs(output_dir, exist_ok=True)
        train.to_csv(f"{output_dir}/train.csv", index=False)
        val.to_csv(f"{output_dir}/val.csv", index=False)
        test.to_csv(f"{output_dir}/test.csv", index=False)

        print(f"\n✅ Dataset created:")
        print(f"  Train: {len(train)}")
        print(f"  Val: {len(val)}")
        print(f"  Test: {len(test)}")

        return train, val, test

# 실행
if __name__ == "__main__":
    import os
    builder = DatasetBuilder()

    train, val, test = builder.build_final_dataset(
        unsmile_path="data/unsmile_dataset.csv",
        custom_path="data/custom_labeled_data.csv",
        output_dir="dataset"
    )
```

**실행**:
```bash
python preprocessing/dataset_builder.py
```

---

## 📋 Week 2: 모델 학습 및 배포 (Day 6-10)

### Day 6-7: 모델 학습

#### ✅ 체크리스트
- [ ] 학습 설정 파일 작성
- [ ] 학습 스크립트 작성
- [ ] Freeze → Unfreeze 2단계 학습 실행

#### 🔧 구현 코드

**training/config.yaml**:
```yaml
# 모델 설정
model:
  name: "smilegate-ai/kor_unsmile"
  num_labels: 11  # 10 + negative_morale

# 학습 설정
training:
  batch_size: 32
  num_epochs_freeze: 5   # Base 모델 freeze
  num_epochs_full: 5     # 전체 fine-tune
  learning_rate_base: 1e-6
  learning_rate_head: 1e-3
  weight_decay: 0.01
  warmup_steps: 100

# 데이터
data:
  train_path: "dataset/train.csv"
  val_path: "dataset/val.csv"
  test_path: "dataset/test.csv"
  max_length: 128

# 저장
output:
  checkpoint_dir: "training/checkpoints"
  model_dir: "models/unsmile_v1.0"
```

**training/train.py** (계속됩니다...):
```python
import torch
import torch.nn as nn
from torch.utils.data import Dataset, DataLoader, WeightedRandomSampler
from transformers import (
    AutoTokenizer,
    AutoModelForSequenceClassification,
    TrainingArguments,
    Trainer
)
from sklearn.utils.class_weight import compute_class_weight
import pandas as pd
import numpy as np
import yaml

class UnsmileDataset(Dataset):
    def __init__(self, texts, labels, tokenizer, max_length=128):
        self.texts = texts
        self.labels = labels
        self.tokenizer = tokenizer
        self.max_length = max_length

    def __len__(self):
        return len(self.texts)

    def __getitem__(self, idx):
        text = str(self.texts[idx])
        label = self.labels[idx]

        encoding = self.tokenizer(
            text,
            max_length=self.max_length,
            padding='max_length',
            truncation=True,
            return_tensors='pt'
        )

        return {
            'input_ids': encoding['input_ids'].flatten(),
            'attention_mask': encoding['attention_mask'].flatten(),
            'labels': torch.tensor(label, dtype=torch.long)
        }

class UnsmileTrainer:
    def __init__(self, config_path="config.yaml"):
        # Config 로드
        with open(config_path, 'r') as f:
            self.config = yaml.safe_load(f)

        # Tokenizer & Model
        self.tokenizer = AutoTokenizer.from_pretrained(self.config['model']['name'])
        self.model = AutoModelForSequenceClassification.from_pretrained(
            self.config['model']['name'],
            num_labels=self.config['model']['num_labels']
        )

        # GPU 설정
        self.device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
        self.model.to(self.device)
        print(f"Using device: {self.device}")

    def load_data(self):
        """데이터 로드"""
        train_df = pd.read_csv(self.config['data']['train_path'])
        val_df = pd.read_csv(self.config['data']['val_path'])

        self.train_dataset = UnsmileDataset(
            train_df['text'].tolist(),
            train_df['label'].tolist(),
            self.tokenizer,
            self.config['data']['max_length']
        )

        self.val_dataset = UnsmileDataset(
            val_df['text'].tolist(),
            val_df['label'].tolist(),
            self.tokenizer,
            self.config['data']['max_length']
        )

        # Class weights 계산
        self.class_weights = compute_class_weight(
            'balanced',
            classes=np.unique(train_df['label']),
            y=train_df['label']
        )
        self.class_weights = torch.tensor(self.class_weights, dtype=torch.float).to(self.device)

        print(f"Train: {len(self.train_dataset)}, Val: {len(self.val_dataset)}")

    def train_stage1_freeze(self):
        """Stage 1: Base 모델 Freeze, Head만 학습"""
        print("\n" + "="*50)
        print("Stage 1: Training with Base Model Frozen")
        print("="*50)

        # Base 모델 Freeze
        for param in self.model.roberta.parameters():
            param.requires_grad = False

        # 학습 설정
        training_args = TrainingArguments(
            output_dir=self.config['output']['checkpoint_dir'] + "/stage1",
            num_train_epochs=self.config['training']['num_epochs_freeze'],
            per_device_train_batch_size=self.config['training']['batch_size'],
            per_device_eval_batch_size=self.config['training']['batch_size'],
            learning_rate=self.config['training']['learning_rate_head'],
            weight_decay=self.config['training']['weight_decay'],
            warmup_steps=self.config['training']['warmup_steps'],
            evaluation_strategy="epoch",
            save_strategy="epoch",
            load_best_model_at_end=True,
            logging_dir='./logs',
        )

        # Weighted Loss
        class WeightedLossTrainer(Trainer):
            def compute_loss(self, model, inputs, return_outputs=False):
                labels = inputs.pop("labels")
                outputs = model(**inputs)
                logits = outputs.logits
                loss_fct = nn.CrossEntropyLoss(weight=self.class_weights)
                loss = loss_fct(logits, labels)
                return (loss, outputs) if return_outputs else loss

        WeightedLossTrainer.class_weights = self.class_weights

        trainer = WeightedLossTrainer(
            model=self.model,
            args=training_args,
            train_dataset=self.train_dataset,
            eval_dataset=self.val_dataset,
        )

        trainer.train()
        print("✅ Stage 1 completed")

    def train_stage2_full(self):
        """Stage 2: 전체 모델 Fine-tune"""
        print("\n" + "="*50)
        print("Stage 2: Full Model Fine-tuning")
        print("="*50)

        # Base 모델 Unfreeze
        for param in self.model.roberta.parameters():
            param.requires_grad = True

        # 차등 학습률
        optimizer_grouped_parameters = [
            {
                "params": self.model.roberta.parameters(),
                "lr": self.config['training']['learning_rate_base']
            },
            {
                "params": self.model.classifier.parameters(),
                "lr": self.config['training']['learning_rate_head']
            }
        ]

        optimizer = torch.optim.AdamW(
            optimizer_grouped_parameters,
            weight_decay=self.config['training']['weight_decay']
        )

        # 학습 설정
        training_args = TrainingArguments(
            output_dir=self.config['output']['checkpoint_dir'] + "/stage2",
            num_train_epochs=self.config['training']['num_epochs_full'],
            per_device_train_batch_size=self.config['training']['batch_size'],
            per_device_eval_batch_size=self.config['training']['batch_size'],
            weight_decay=self.config['training']['weight_decay'],
            evaluation_strategy="epoch",
            save_strategy="epoch",
            load_best_model_at_end=True,
            logging_dir='./logs',
        )

        # Weighted Loss Trainer (동일)
        class WeightedLossTrainer(Trainer):
            def compute_loss(self, model, inputs, return_outputs=False):
                labels = inputs.pop("labels")
                outputs = model(**inputs)
                logits = outputs.logits
                loss_fct = nn.CrossEntropyLoss(weight=self.class_weights)
                loss = loss_fct(logits, labels)
                return (loss, outputs) if return_outputs else loss

        WeightedLossTrainer.class_weights = self.class_weights

        trainer = WeightedLossTrainer(
            model=self.model,
            args=training_args,
            train_dataset=self.train_dataset,
            eval_dataset=self.val_dataset,
            optimizers=(optimizer, None)
        )

        trainer.train()
        print("✅ Stage 2 completed")

    def save_model(self):
        """최종 모델 저장"""
        output_dir = self.config['output']['model_dir']
        self.model.save_pretrained(output_dir)
        self.tokenizer.save_pretrained(output_dir)
        print(f"✅ Model saved to {output_dir}")

    def run(self):
        """전체 학습 파이프라인"""
        self.load_data()
        self.train_stage1_freeze()
        self.train_stage2_full()
        self.save_model()

# 실행
if __name__ == "__main__":
    trainer = UnsmileTrainer(config_path="config.yaml")
    trainer.run()
```

**실행**:
```bash
cd training
python train.py
```

---

### Day 8-9: 평가 및 분석

#### ✅ 체크리스트
- [ ] 테스트 데이터로 평가
- [ ] F1-score, Confusion Matrix 산출
- [ ] 성공 기준 달성 여부 확인

#### 🔧 평가 코드

**evaluation/evaluate.py**:
```python
import torch
from transformers import AutoTokenizer, AutoModelForSequenceClassification
from sklearn.metrics import classification_report, confusion_matrix, f1_score
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

class ModelEvaluator:
    def __init__(self, model_path, test_data_path):
        self.tokenizer = AutoTokenizer.from_pretrained(model_path)
        self.model = AutoModelForSequenceClassification.from_pretrained(model_path)
        self.device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
        self.model.to(self.device)
        self.model.eval()

        # 테스트 데이터 로드
        self.test_df = pd.read_csv(test_data_path)

        # 라벨 이름 (11개)
        self.label_names = [
            'clean', '여성/가족', '남성', '성소수자', '인종/국적',
            '연령', '지역', '종교', '기타 혐오', '악플/욕설', 'negative_morale'
        ]

    def predict(self, text):
        """단일 텍스트 예측"""
        encoding = self.tokenizer(
            text,
            max_length=128,
            padding='max_length',
            truncation=True,
            return_tensors='pt'
        )

        with torch.no_grad():
            outputs = self.model(
                input_ids=encoding['input_ids'].to(self.device),
                attention_mask=encoding['attention_mask'].to(self.device)
            )
            logits = outputs.logits
            pred = torch.argmax(logits, dim=1).cpu().numpy()[0]

        return pred

    def evaluate(self):
        """전체 평가"""
        print("Evaluating on test set...")

        y_true = self.test_df['label'].tolist()
        y_pred = []

        for text in self.test_df['text']:
            pred = self.predict(text)
            y_pred.append(pred)

        # Classification Report
        print("\n" + "="*60)
        print("Classification Report")
        print("="*60)
        report = classification_report(
            y_true, y_pred,
            target_names=self.label_names,
            digits=3
        )
        print(report)

        # F1-scores
        f1_macro = f1_score(y_true, y_pred, average='macro')
        f1_weighted = f1_score(y_true, y_pred, average='weighted')

        print(f"\nMacro F1-Score: {f1_macro:.3f}")
        print(f"Weighted F1-Score: {f1_weighted:.3f}")

        # negative_morale F1
        negative_morale_idx = 10
        f1_per_class = f1_score(y_true, y_pred, average=None)
        print(f"negative_morale F1-Score: {f1_per_class[negative_morale_idx]:.3f}")

        # Confusion Matrix
        cm = confusion_matrix(y_true, y_pred)
        self.plot_confusion_matrix(cm)

        # 성공 기준 체크
        print("\n" + "="*60)
        print("Success Criteria Check")
        print("="*60)

        criteria_met = {
            "negative_morale F1 ≥ 0.70": f1_per_class[negative_morale_idx] >= 0.70,
            "Average F1 ≥ 0.80": f1_macro >= 0.80,
            "negative_morale F1 ≥ 0.75 (목표)": f1_per_class[negative_morale_idx] >= 0.75,
        }

        for criterion, met in criteria_met.items():
            status = "✅ PASS" if met else "❌ FAIL"
            print(f"{status} - {criterion}")

    def plot_confusion_matrix(self, cm):
        """Confusion Matrix 시각화"""
        plt.figure(figsize=(12, 10))
        sns.heatmap(
            cm,
            annot=True,
            fmt='d',
            cmap='Blues',
            xticklabels=self.label_names,
            yticklabels=self.label_names
        )
        plt.title('Confusion Matrix')
        plt.ylabel('True Label')
        plt.xlabel('Predicted Label')
        plt.tight_layout()
        plt.savefig('evaluation/confusion_matrix.png', dpi=300)
        print("\n✅ Confusion matrix saved to evaluation/confusion_matrix.png")

# 실행
if __name__ == "__main__":
    evaluator = ModelEvaluator(
        model_path="../models/unsmile_v1.0",
        test_data_path="../dataset/test.csv"
    )
    evaluator.evaluate()
```

**실행**:
```bash
python evaluation/evaluate.py
```

---

### Day 10: 프론트엔드 최적화 및 마무리

#### ✅ 체크리스트
- [ ] Web Worker STT 처리 분리
- [ ] 최종 모델 배포 준비
- [ ] 문서화 및 README 작성

#### 🔧 Web Worker 코드

**frontend/stt-worker.js**:
```javascript
// Web Worker에서 실행될 STT 처리 로직

self.onmessage = function(e) {
    const { transcript, timestamp } = e.data;

    // 1. 전처리
    const processedText = preprocessText(transcript);

    // 2. API 호출 (백엔드 모델 추론)
    fetch('http://localhost:5000/predict', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ text: processedText })
    })
    .then(response => response.json())
    .then(data => {
        // 3. 결과 메인 스레드로 전송
        self.postMessage({
            label: data.label,
            confidence: data.confidence,
            timestamp: timestamp
        });
    })
    .catch(error => {
        self.postMessage({ error: error.message });
    });
};

function preprocessText(text) {
    // 간단한 전처리
    return text.trim().toLowerCase();
}
```

**frontend/main.js**:
```javascript
// 메인 스레드

const sttWorker = new Worker('stt-worker.js');

// Web Speech API 설정
const recognition = new webkitSpeechRecognition();
recognition.continuous = true;
recognition.interimResults = false;
recognition.lang = 'ko-KR';
recognition.maxAlternatives = 1;

recognition.onresult = (event) => {
    const transcript = event.results[event.results.length - 1][0].transcript;

    // Worker로 처리 위임 (메인 스레드 즉시 해방)
    sttWorker.postMessage({
        transcript: transcript,
        timestamp: Date.now()
    });
};

// Worker 결과 처리
sttWorker.onmessage = (e) => {
    if (e.data.error) {
        console.error('STT Error:', e.data.error);
        return;
    }

    // 페널티 적용
    if (e.data.label === 'negative_morale' || e.data.label.includes('욕설')) {
        applyPenalty(e.data);
    }
};

function applyPenalty(data) {
    console.log(`페널티 적용: ${data.label} (신뢰도: ${data.confidence})`);
    // 게임 로직 (저주 부여 등)
}

// 시작
recognition.start();
```

---

## 📊 최종 체크리스트

### Week 1 완료 조건
- [ ] VAD 청킹 코드 작동 확인
- [ ] 500-700개 STT 데이터 수집 완료
- [ ] 데이터 증강 및 정규화 적용
- [ ] 라벨링 완료 및 CSV 저장
- [ ] Train/Val/Test 분할 완료

### Week 2 완료 조건
- [ ] Stage 1 학습 완료 (Freeze)
- [ ] Stage 2 학습 완료 (Full fine-tune)
- [ ] 평가 F1-score ≥ 0.70 (negative_morale)
- [ ] Confusion Matrix 생성
- [ ] Web Worker 적용 및 렉 개선 확인

### 배포 준비
- [ ] 모델 파일 저장 (`models/unsmile_v1.0/`)
- [ ] README.md 작성
- [ ] API 서버 구축 (Flask/FastAPI)
- [ ] 프론트엔드 통합 테스트

---

## 🚨 트러블슈팅

### 자주 발생하는 문제

1. **GPU 메모리 부족**
   ```bash
   # 배치 사이즈 줄이기
   batch_size: 32 → 16
   ```

2. **STT API Rate Limit**
   ```python
   # 요청 간 딜레이 추가
   import time
   time.sleep(0.5)
   ```

3. **F1-score가 목표 미달**
   - 데이터 품질 재확인
   - 라벨링 일관성 검토
   - 오버샘플링 비율 조정 (5배 → 10배)

---

## 📞 긴급 연락처 (팀 내부)

- 데이터 수집 담당: [이름]
- 모델 학습 담당: [이름]
- 프론트엔드 담당: [이름]

---

**이 문서는 매일 업데이트하며, 체크리스트를 완료할 때마다 표시하세요!**
